{
  "name": "@praxis/ledger",
  "version": "0.6.0",
  "private": false,
  "type": "module",
  "description": "PRAXIS append-only Merkle evidence ledger with atomic writes and crash recovery.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "bun run tsc --noEmit"
  },
  "dependencies": {
    "@praxis/protocol": "file:../protocol"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "bun": ">=1.0.0"
  }
}
