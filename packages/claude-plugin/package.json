{
  "name": "@praxis/claude-plugin",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "PRAXIS Claude Code plugin bridge — display + dispatch for Truth Kernel verification. Read-only. Never decides truth.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "bun run tsc --noEmit",
    "test": "bun test",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@praxis/kernel": "file:../kernel",
    "@praxis/contracts": "file:../contracts",
    "@praxis/verity-gates": "file:../verity-gates"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "bun": ">=1.0.0"
  }
}
