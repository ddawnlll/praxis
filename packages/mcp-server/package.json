{
  "name": "@praxis/mcp-server",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "PRAXIS MCP Server — Model Context Protocol server for autonomous agent integration",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "bun run tsc --noEmit",
    "test": "bun test",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@praxis/kernel": "file:../kernel",
    "@praxis/contracts": "file:../contracts"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "bun": ">=1.0.0"
  }
}
